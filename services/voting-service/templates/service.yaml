apiVersion: v1
kind: Service
metadata:
  name: {{ include "voting-service.fullname" . }}
  labels:
    {{- include "voting-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "voting-service.selectorLabels" . | nindent 4 }}
